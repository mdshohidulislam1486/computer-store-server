export type TProduct = {
  name: string;
  price: number;
  quantity: string;
  category: string;
  brand: string;
  compatibility: string;
  interface: string;
  condition: string;
  capacity: string;
  color: string;
  image: string;
};
